name: Deploy ON Push

on:
    push:
        branches:
            - main

jobs:
    check:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v4

            - name: HealthCheck
              run: echo "Health Check Passed"

